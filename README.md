#create venv

python -m venv .venv
.venv\Scripts\activate


#use venv



 
pip install dotenv transformers datasets accelerate torch scikit-learn matplotlib



1. save the data sets, IBM, locally 22,500 and 2,500 rows
2. save the untrained model, bert-base-uncased, locally
3. save the trained model    
4. fine tunes the model 
5. evaluates the model


